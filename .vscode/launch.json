{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-msedge",
      "request": "launch",
      "name": "Abrir en Edge (limpio)",
      "url": "http://localhost:5500",
      "webRoot": "${workspaceFolder}"
    }
  ]
}